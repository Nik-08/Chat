import styled from "styled-components";

export const ChatFooter = styled.footer`
  width: 90%;
  padding-bottom: 10px;
  position: absolute;
  left: 50%;
  bottom: 0;
  transform: translateX(-50%);
`;
